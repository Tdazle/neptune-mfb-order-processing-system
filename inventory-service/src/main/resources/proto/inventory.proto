syntax = "proto3";
service InventoryService {
  /**
   * Check if the requested product is available in the specified quantity.
   */
  rpc CheckStock(StockRequest) returns (StockResponse);
}
message StockRequest {
  string product = 1;
  int32 quantity = 2;
}
message StockResponse {
  bool available = 1;
}